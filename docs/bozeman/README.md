# Bozeman RFP — Requirements Tracker

**City:** Bozeman, Montana
**RFP:** Utility Billing Software
**Proposed solution:** SaaSLogic Utilities + SaaSLogic Billing
**Status:** Proposal preparation

This folder collects the formal requirements documents for every commitment we make in the Bozeman RFP response. Each numbered file translates one or more RFP claims into testable requirements with acceptance criteria, implementation approach, and current-state gap analysis.

The point: every claim we make to the City should map to a requirements doc here so the engineering team can scope, build, and verify against it. If a claim doesn't map to a doc, it shouldn't be in the RFP response.

## Index

| # | Topic | RFP claim summary | Status |
|---|---|---|---|
| 01 | [Audit & Tamper-Evidence](./01-audit-and-tamper-evidence.md) | Append-only audit log, S3 replication with object-lock, daily Merkle hash chains, security-event coverage, two-person approval for sensitive changes | **Drafted — implementation pending** |
| 02 | [Mobile & Responsive UI](./02-mobile-and-responsive-ui.md) | Three-tier mobile strategy: CSR workflows fully mobile (Tier 1), power-user workflows desktop-optimized + tablet-usable (Tier 2), field-technician workflows mobile-first with offline + photo + GPS + barcode (Tier 3) | **Drafted — implementation pending** |
| 03 | [Progressive Web App (PWA)](./03-progressive-web-app.md) | Manifest + installability across admin/portal/field surfaces, service worker with per-resource cache strategies, offline shell, IndexedDB queue + background sync for field surface, update lifecycle with force-reload for security patches, iOS Safari constraints. Push notifications explicitly out of scope. | **Drafted — implementation pending** |
| 04 | [Attachments](./04-attachments.md) | Upload/view/download/delete on 7 entity types (current: 5 — adds ServiceRequest + Adjustment), expanded MIME types (adds PowerPoint + HEIC + TIFF), full audit trail (currently absent), document categories, soft-delete with configurable purge, multi-field search by filename/description/uploader/date/parent, S3 storage with KMS + pre-signed URLs + lifecycle archival to lower-cost tiers, **Apptorflow-mediated DMS integration (SharePoint/Laserfiche/OnBase/M-Files)** with two patterns (external pointer or internal replication), **per-entity-type-and-category retention policies** with dual-approved purges and immutable signed manifests. Native FTS explicitly roadmap. | **Drafted — partial implementation (basic CRUD exists for 5 entities)** |
| 05 | [Customer Portal](./05-customer-portal.md) | ~30-capability surface: bill view (PDF + itemized), consumption charts, one-time + scheduled payments + AutoPay + payment plans, paperless billing, service start/stop/transfer, SR intake + tracking, tax docs, in-portal messaging, contact preferences. Multi-account login with one-click switching across multiple Customers; landlord/property-mgmt portfolio access; audit-logged delegation. Notifications via email + SMS + in-portal + web push (portal only) + voice via Apptorflow; templated, event-configurable, per-resident preferences. Largest scope in the proposal. | **Drafted — minimal implementation (login/dashboard/profile/usage exist; bills are mock; no payments/SRs/messaging/delegation/multi-account/notifications)** |
| 06 | [Custom Fields](./06-custom-fields.md) | Per-tenant custom fields on all major entities through Settings module. Adds **ServiceRequest** entity (5 currently supported); **currency** + **multi-select** data types (5 currently); validation rules beyond required (min/max, length, regex, date bounds); **conditional visibility** with safe DSL; **default values**; drag-and-drop ordering. Custom fields auto-appear in: bulk API exports (CSV/Excel/JSON), per-tenant OpenAPI variant, 8 pre-built standard reports, ad-hoc query builder with saved queries, scheduled reports. Field-level RBAC and per-tenant variants explicitly out of scope (Phase 4). | **Drafted — most-built RFP capability so far (Phases 1+2 complete on 5 entities; reporting + query-builder are the dominant gap)** |
| 07 | [Data Validation](./07-data-validation.md) | Four-tier validation: **field-level** (type/format/range/required/regex — strong via Zod across 32 validators), **cross-field** (start-date before end-date, dependent picklists — partial, only 7 of 32 use `.refine()`), **entity-level** (a premise must have at least one customer — limited; `Premise.ownerId` is currently nullable so the RFP example actually fails today), **integration-time** (meter reads validated against premise eligibility and prior reads — partial; rollover/defect/reverse exist, premise eligibility check is missing). Real-time UX surfacing with **actionable** messages (replaces generic Zod text), `<ValidatedField>` wrapper for inline errors, multi-error display. Adds entity-rules registry, `ImportBatchError` + `DataQualityViolation` tables, makes `Premise.ownerId` NOT NULL. | **Drafted — partial implementation (Tier 1 strong; Tiers 2-4 have gaps; UX is submit-time only)** |
| 08 | [Data Retention, Archival & Purge](./08-data-retention-archival-purge.md) | Indefinite default retention; `retention_policy` table per `(tenant, retentionClass, documentCategory)` with statutory floors enforced in product code (FINANCIAL/AUDIT_FINANCIAL/AUDIT_SECURITY = 7 yrs, METER_READ = 5 yrs); `legal_hold` table replaces unused `Account.isProtected`. Archival pipeline: declarative range-partitioned `meter_read`/`audit_log`/`notification_log` exported to Parquet on S3 (Standard → IA → Glacier IR → Glacier Deep Archive); closed-account graphs exported per-account; metadata stays hot for searchability; reports federate via Athena/DuckDB. Purge engine: retention-driven (nightly worker) + operator-requested (UI) + subject-requested (GDPR/CCPA partial-erasure with name redaction over retained financial records); dual-approval via generalized `pending_administrative_change` table; **immutable signed purge manifests** in S3 object-lock-compliance bucket, chained via `previousManifestSha256`, verified by external `saaslogic-verify-purge-chain` CLI. Statutory floors override every policy edit. | **Drafted — minimal implementation (only scheduler-audit retention sweep is built; `Account.isProtected` exists but is never enforced)** |
| 09 | [Bulk Upload & Data Ingestion](./09-bulk-upload-and-data-ingestion.md) | Three channels (UI CSV/Excel, API JSON/XML, SFTP scheduled drops) converging into one channel-agnostic pipeline core (parse → validate → stage → commit → report → audit). `EntityIngestor` registry covers MeterRead, Payment, Adjustment, Commodity, RateSchedule, Premise, Meter, Customer; reference-data defaults to upsert, operational-data to insert-only. Validation gate is non-bypassable, runs Tier 1-4 from doc 07, collects all per-row errors (max 10 per row), produces actionable messages (never raw Zod text). Per-entity error policies (`reject_rows` for meter reads, `reject_batch` for rate schedules, `reject_batch_threshold:0` for payments). Three idempotency layers: file-SHA-256 (SFTP), `Idempotency-Key` header (API), per-row natural key (all channels). SFTP via `ssh2-sftp-client` with KMS-referenced credentials and pinned host fingerprints; poller deduplicates by SHA-256, separate `import-processor` worker handles parsing. Validation report (success/failure counts + per-row reasons) persisted to `import_batch_report` + S3 + audit log. Streaming-first for memory safety on multi-GB files. Per-tenant quotas. | **Drafted — substantial gaps (the existing meter-read import UI waits on a backend that doesn't exist; zero XML/Excel/SFTP infrastructure; ImportBatch table is unused)** |
| 10 | [Draft Status & Posting](./10-draft-status-and-posting.md) | **Drafts are a status, not a parallel set of tables.** Each adopted entity's existing status enum is extended with `DRAFT`, `PENDING_APPROVAL`, `DISCARDED`. Shared draft-metadata columns (originator, payloadVersion, lastSavedAt, visibility, etc.) added to each adopted entity. Existing NOT-NULL columns relaxed and a CHECK constraint reinstates them when status leaves draft. `DraftVisibility` (ORIGINATOR_ONLY default, SHARED_WITH_NAMED_USERS, SHARED_WITH_ROLE, TENANT_WIDE) enforced via **three-layer scoping**: extended Postgres RLS using `app.current_user_id`, service-layer `withProductionScope()` / `withDraftVisibility()` helpers, per-entity `<entity>_v` view. Debounced autosave (2s after keystroke or 30s timer) with optimistic-locking via `payloadVersion` and three-way merge UI on 409; IndexedDB offline queue. **Post pipeline is a single `UPDATE status` in one transaction** — no insert-from-draft saga, no `posted_as_id` redirection, no mid-post recovery code. High-stakes entities (rate schedules, billing-cycle parameters, large adjustments) move to `PENDING_APPROVAL` and create a `pending_administrative_change` row; second approval promotes to first-active state. **Drafts excluded from production reads, schedulers, reports, FKs, custom-fields exports** via `no-raw-entity-query` linter rule + production view. Three polymorphic shared tables (`draft_collaborator`, `draft_role_grant`, `draft_lock`) cover cross-entity collaboration. Adopted entities: SR, Adjustment (when Module 10 ships), BillingCycle, RateSchedule, Customer, Premise, Meter, ServiceAgreement, NotificationTemplate, RetentionPolicy (from doc 08), CustomFieldDefinition (from doc 06). Distinct from `pending_administrative_change` (approval gate, not WIP). | **Drafted — minimal implementation (zero entities have explicit DRAFT status; Adjustment entity doesn't exist; no client-side autosave; `createdBy` columns exist but are never filtered)** |
| 11 | [Notes & Comments](./11-notes-and-comments.md) | First-class comments on all major entities via a single polymorphic `comment` table (`(entity_type, entity_id)` — same pattern as the existing Attachment model). Markdown source + server-rendered sanitized HTML cache (Tiptap editor on the client, `unified`/`remark`/`rehype-sanitize` on the server; raw HTML rejected). Subset: bold/italic/code/lists/links/blockquote/strikethrough; no headings, tables, images, raw HTML. **`@`-mentions stored structurally** (`comment_mention` junction with `mentioned_user_id`) — rename-resilient; on save, mentioned users get `IN_APP` notifications within 30s p99 via a new notification channel. **Internal-only by default** (`isInternal = true`); flipping to customer-visible requires a separate permission and is **not reversible** (customer may already have read it). Customer-visible comments surface in portal as "Messages from Utility" with `COMMENT_FROM_UTILITY` notifications and per-tenant rate limits. Polymorphic Attachment model extended with `"comment"` entity type — same upload/download/audit pipeline. FTS via the existing `tsvector` + GIN pattern proven on Customer/Premise/Account/Meter. Audit auto-emitted via existing `auditCreate` wrapper; edits to delivered customer-visible comments emit additional `AUDIT_SECURITY` rows. Bell-icon UI in admin + portal top bars; `/inbox` page; per-user notification preferences. | **Drafted — minimal implementation (zero Comment/Note tables; only scattered free-text columns on 8 entities; no rich-text editor; no @-mention parsing; no in-app notification channel)** |
| 12 | [Corrections & Reversals](./12-corrections-and-reversals.md) | Posted financial transactions are immutable on monetary fields (CHECK + `before_update_financial` trigger reject UPDATE on amount/period when status is posted); corrections are issued as **separate traceable entities** with `reverses_id` FK to the original. Two distinct mechanisms: **reversal** (creates inverse posting; transitions original status to VOID/REVERSED in same transaction; terminal — reversal of a reversal is forbidden) vs. **adjustment** (additive AdhocCharge that doesn't undo any specific prior transaction; itself reversible). First-class operations: bill void, payment reversal, NSF return (three-row pattern: Payment-status-update + reversed-Payment + NSFRecord + auto-posted AdhocCharge for fee — idempotent on SaaSLogic webhook event ID), adjustment reversal, write-off reversal. Seven per-tenant reason-code reference tables (BillVoidReasonDef / PaymentReversalReasonDef / NSFReasonDef / AdjustmentReasonDef / AdjustmentReversalReasonDef / WriteOffReasonDef / WriteOffReversalReasonDef) with `requiresApprovalAt` thresholds + tenant-config dollar thresholds; dual approval via doc 08's `pending_administrative_change` table (originator ≠ approver enforced). **Self-reversal always dual-approved regardless of amount.** Every operation emits `event_class = AUDIT_FINANCIAL` audit rows in the same transaction (no outbox/saga — atomicity required); 7-year statutory retention floor from doc 08 applies. **Net-effect reporting** sums originals + reversals via materialized `Account.balance` updated in the same transaction as the financial event; nightly reconciliation alerts on drift > $0.01. Customer portal surfaces reversals with per-tenant `customerFacingLabel`. **Architectural delegation:** SaaSLogic owns the payment ledger and originates payment reversals via webhook; CIS owns adjustments + bill voids + NSF posting + the audit/UI/reporting around all of it. | **Drafted — virtually no implementation (zero financial entities exist; Module 09/10 are Phase 3 stubs; SaaSLogic webhook handlers not built; Account.balance is never written by any code path; audit lacks event_class=FINANCIAL classification)** |
| 13 | [Workflow, Approvals & Action Queue](./13-workflow-approvals-action-queue.md) | Bundles **six related RFP claims** (workflow rule builder, user notifications, in-system comms, workflow visibility, delegation, escalation) into one feature because they share substrate. **No-code rule builder** in `/settings/workflows` (React Flow drag-drop) with bounded JSON DSL: triggers (entity_created / status_changed / field_updated / scheduled / external_event via Apptorflow), conditions (field_compare / actor_role_in / value_threshold + AND/OR/NOT groups), actions (assign / notify / set_status / set_field / create_child / create_approval_request / call_external). `WorkflowDefinition` + immutable `WorkflowVersion` (DRAFT → ACTIVE → RETIRED with rollback); high-stakes workflows require dual-approval to publish. Hardcoded SR `VALID_TRANSITIONS` map migrated to a workflow row. **`Task` action queue** (polymorphic — replaces ad-hoc per-entity assignment lists) + `/my-tasks` page with Open/In Progress/Recently Completed tabs; auto-completes on source-entity terminal transition. **Per-user, per-category notification preferences** (assignment / mention / escalation / customer_message / system / digest / broadcast) with channel × cadence matrix; product-fixed categories. **Email digest worker** runs daily/weekly per user preference. **Dashboard widgets** (My Tasks, Aged Approvals, Team Workload, Recent Mentions, System Health). **Workflow visibility** on every entity detail: current-state chip + transition history (driven by audit_log) + pending tasks + visual workflow path (React Flow read-only). **Approver delegation**: `Delegation` table with start/end dates + maxDollarAmount cap + scope; chained/self-delegation forbidden; audit captures both `acted_by_user_id` and `authority_of_user_id`. **Org chart**: `CisUserManager` table (separate from CisUser, multi-domain support — financial vs operational managers); `/settings/org-chart` UI; CSV import via doc 09; cycle detection. **Escalation**: extends `pending_administrative_change` with `slaResponseHours` + `escalationConfig`; 5-min `escalation-worker` ticks; reminder notifications at configurable thresholds; escalates to alternate user / role / `manager_of(user)`. **Broadcasts**: separate `Broadcast` entity (operators-only audience; customer broadcasts use a different path). All audit emission stays in-transaction with the entity mutation (no outbox). | **Drafted — minimal implementation (workflow engine ✗; My Tasks ✗; daily digest is a toggle stub; UserPreference.preferences is empty JSON never read; no broadcast; no workflow viz; no managerId on CisUser; no Delegation; SLA escalation columns exist but worker doesn't read them; pending_administrative_change still proposed-not-built across docs 01/08/10/12)** |

## Conventions

- **Numbered prefix** (`01-`, `02-`, …) keeps the index ordered and stable. Re-numbering invalidates citations in the RFP response.
- **Filenames** use kebab-case. The first line of each doc is the topic title.
- **Requirement IDs** follow `FR-<DOMAIN>-<NNN>` for functional and `NFR-<DOMAIN>-<NNN>` for non-functional. Cite these in code comments and test names so traceability holds.
- **"Out of scope"** sections are mandatory in each doc. Naming what we are not committing to is as important as naming what we are.
- **Current-state gap** sections cite specific files and line numbers where applicable so the gap is auditable.

## Process

1. Each RFP claim is captured verbatim in the requirements doc that owns it.
2. Implementation effort estimates carry a confidence band (S / M / L / XL).
3. Acceptance criteria must be testable — automated where reasonable, documented manual verification otherwise.
4. The "Out of scope" section is signed off by the proposal owner before the RFP response is finalized.

## SaaSLogic product positioning

- **SaaSLogic Utilities** — the customer information system (CIS) covering customer/account/premise/agreement/meter/SR domains. This is the codebase in `packages/api` and `packages/web`.
- **SaaSLogic Billing** — the billing engine. Proposed integration documented in `docs/specs/21-saaslogic-billing.md` (Phase 3).

The Bozeman proposal positions both as a single deployable solution. Where requirements span both products, the doc should call out which side owns each piece.
